# subarrays have same number of elements
# shift grid k times

# a.unshift(elem)
# a.pop -> returns last element

# IN : 2D ARRAY grid, INT k
# OUT: 2D ARRAY shifted k times


def shift_grid(grid, k)
  # k times do: pop last and unshift to first
  # pop first and unshift to next
  # repeat until second-to-last array
end
